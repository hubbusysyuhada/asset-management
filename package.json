{
  "name": "asset-management",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ts-node generator.ts && npx tsup && rm -rf ./dist/icons && rm -rf ./dist/illustrations && rm -rf ./dist/special-icons && rm -rf ./dist/icon-illustrations && cp -r ./Vue/* ./dist/",
    "publish": "cd ./dist && npm publish && cd ..",
    "build-and-publish": "npm run build && npm run publish"
  },
  "keywords": [],
  "author": "Muhammad Hafidz Hubbusysyuhada",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/lodash": "^4.14.202",
    "lodash": "^4.17.21",
    "tsup": "^8.0.1"
  }
}
